<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.rocbillow.doraemon">

    <application
        android:name="com.rocbillow.doraemon.DoraemonApp"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.App">
        <activity android:name="com.rocbillow.doraemon.ui.arch.sunflower.GardenActivity" />
        <activity
            android:name="com.rocbillow.doraemon.ui.main.MainActivity"
            android:launchMode="singleTask" />
        <activity
            android:name="com.rocbillow.doraemon.ui.splash.SplashActivity"
            android:theme="@style/Theme.Splash">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>