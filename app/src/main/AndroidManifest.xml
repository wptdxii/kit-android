<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.olaroc.doraemon">

    <application
        android:name="com.olaroc.doraemon.DoraemonApp"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.App"
        tools:ignore="AllowBackup">
        <activity
            android:name="com.olaroc.doraemon.ui.main.MainActivity"
            android:launchMode="singleTask" />
        <activity
            android:name="com.olaroc.doraemon.ui.splash.SplashActivity"
            android:theme="@style/Theme.Splash">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <provider
            android:name="androidx.startup.InitializationProvider"
            android:authorities="${applicationId}.androidx-startup"
            android:exported="false"
            tools:node="merge">
            <meta-data
                android:name="com.olaroc.doraemon.initializer.AppInitializer"
                android:value="androidx.startup" />
        </provider>
    </application>

</manifest>